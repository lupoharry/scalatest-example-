object CubeCalculator {
  def cube(x: Int): Int =
    x * x * x
}
// This is a simple Scala object that defines a method `cube` which takes an integer `x` and returns its cube by multiplying `x` by itself three times.